spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zephyrpawan/SmartHomeHub-Configs
          search-paths: sprinkler-service
          default-label: main

server:
  port: 8071